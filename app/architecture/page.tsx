import { ArrowLeft, BrainCircuit, CheckCircle2, Database, FileSearch, GitBranch, MessageSquare, Network, Search, Sparkles } from "lucide-react";
const nodes = [['USER PROMPT', 'A natural language question enters the studio.', MessageSquare], ['AGENT DECISION', 'The orchestrator decides if knowledge retrieval is useful.', GitBranch], ['EMBEDDING', 'The query becomes a semantic vector.', BrainCircuit], ['PINECONE', 'Nearest vectors are found with metadata.', Database], ['CONTEXT BUILDER', 'Relevant passages are assembled into the model prompt.', FileSearch], ['LLM GENERATION', 'The configured provider streams a grounded answer.', Sparkles], ['RESULT + CITATIONS', 'The interface updates from the same event stream.', CheckCircle2]] as const;
export default function Architecture() { return <main className="subpage"><header className="sub-header"><a href="/" className="back"><ArrowLeft size={16} /> Back to studio</a><div className="brand small-brand"><span className="brand-mark"><Network size={18} /></span><span>SYSTEM<br /><b>ARCHITECTURE</b></span></div><div className="mode-badge"><span className="live-dot" /> EVENT DRIVEN</div></header><div className="sub-content architecture-content"><div className="sub-title"><div><span className="kicker">HOW IT WORKS</span><h1>One execution. One source of truth.</h1><p>The backend emits typed events over Server-Sent Events. The flow, console, metrics, and chat all render from that stream.</p></div></div><div className="architecture-map">{nodes.map(([title, description, Icon], index) => <div className="arch-node-wrap" key={title}><div className="arch-node"><div className="arch-icon"><Icon size={19} /></div><div><span className="node-number">0{index + 1}</span><h3>{title}</h3><p>{description}</p></div></div>{index < nodes.length - 1 && <div className="arch-connector"><span /><small><Search size={11} /> event</small></div>}</div>)}</div><div className="compare panel-surface"><div className="panel-heading compact"><div><span className="kicker">MODE COMPARISON</span><h2>RAG explains where knowledge enters</h2></div></div><div className="compare-head"><span>CAPABILITY</span><b>DIRECT LLM</b><b>RAG MODE</b></div>{[['Knowledge base', '-', '✓'], ['Vector search', '-', '✓'], ['Embeddings', '-', '✓'], ['Context retrieval', '-', '✓'], ['Source citations', '-', '✓']].map(row => <div className="compare-row" key={row[0]}><span>{row[0]}</span><b className="muted">{row[1]}</b><b className="yes">{row[2]}</b></div>)}<p className="compare-note">RAG can ground responses in retrieved external knowledge when retrieval quality is good. It is not a universal accuracy guarantee.</p></div></div></main> }
