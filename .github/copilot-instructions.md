# RAG AI Chat Studio

- Next.js App Router with TypeScript, Tailwind CSS, Framer Motion, and Lucide React.
- Keep provider credentials server-side; mock mode must be explicit in the UI.
- Validate with `npm run lint`, `npm run test`, and `npm run build`.
- Use the typed event model in `lib/events.ts` for execution state and console output.
